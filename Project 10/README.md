## NodeJS RabbitMQ RPC (TS)

A repository that implements a client implementation with NodeJS and a RabbitMQ server, application, or RPC.

### runs the server

```
npm run start:server
```

### runs the client

```
npm run start:client my_message
```
